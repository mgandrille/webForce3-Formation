{# templates/articles/create.html.twig #}

{% extends "base.html.twig" %}

{% block title %} Blog - Articles {% endblock %}

{% block body %}

<div class="container-fluid">
    <h1> Page de création d'un article</h1>

    <div class="container">
        {# {% if article is defined %}
            <form action="{{ path('article_update', { 'article': article.id }) }}" method="post">
        {% else %}
            <form action="{{ path('article_new') }}" method="post">
        {% endif %}

        <div class="form-group">
            <label for="title">Titre de l'article</label>
            <div class="col-sm-10">
                <input class="form-control" type="text" name="title" id="title" placeholder="Titre" value="{{ article.title | default("") }}">
            </div>
        </div>

        <div class="form-group">
            <label for="short_content">Résumé de l'article</label>
            <div class="col-sm-10">
                <input class="form-control" type="text" name="short_content" id="short_content" placeholder="Résumé" value="{{ article.shortContent | default("") }}">
            </div>
        </div>

        <div class="form-group">
            <label for="content">Rédigez votre Article ici </label>
            <div class="col-sm-10">
                <textarea class="form-control" name="content" id="content" rows="30" placeholder="texte de l'article">{{ article.content | default("") }}</textarea>
            </div>
        </div>

        <input type="submit" value="Enregistrer">

        </form> #}
    
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button>Créer</button>
    {{ form_end(form) }}

    </div>
</div>

{% endblock %}